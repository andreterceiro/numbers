<%= form_with(model: num) do |form| %>
  <% if num.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(num.errors.count, "error") %> prohibited this num from being saved:</h2>

      <ul>
        <% num.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
